export class Job {
  constructor(public title: string, public description: string, public pay:  number, public hours: string, public location: string, public contact: string, public show: boolean, public created: string) { }
}
